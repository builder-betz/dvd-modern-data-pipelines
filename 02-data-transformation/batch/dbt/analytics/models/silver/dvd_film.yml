version: 2

models:
  - name: dvd_film
    tests:
      - unique:
          column_name: film_id
      - not_null:
          column_name: film_id
      - dbt_expectations.expect_table_row_count_to_equal_other_table:
          compare_model: source('dvd_rental', 'raw_dvd_film')
