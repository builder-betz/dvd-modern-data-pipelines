version: 2

models:
  - name: dvd_rental
    tests:
      - unique:
          column_name: rental_id
      - not_null:
          column_name: rental_id
      - dbt_expectations.expect_table_row_count_to_equal_other_table:
          compare_model: source('dvd_rental', 'raw_dvd_rental')
