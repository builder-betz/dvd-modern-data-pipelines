version: 2

models:
  - name: dvd_inventory
    tests:
      - not_null:
          column_name: inventory_id
      - dbt_expectations.expect_table_row_count_to_equal_other_table:
          compare_model: source('dvd_rental', 'raw_dvd_inventory')
